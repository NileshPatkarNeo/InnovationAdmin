@model Innovation_Admin.UI.Models.RemittanceType.RemittanceTypeDto

@{
    Layout = "~/Views/Shared/_AdminHomePage.cshtml";
}
<style>
    .test {
        margin-top: 70px;
    }

    .test2 {
        margin-top: 50px;
    }

    .required {
        color: red;
    }
</style>
<h1 class="text-center test">Create Remittance Type</h1>
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 test2">
            <div class="card bg-light shadow">
                <div class="card-body">
                    <form asp-action="CreateRemittanceType" method="post" id="createCompanyForm">
                        <div class="form-group mb-4">
                            <label asp-for="Name" class="control-label">Name <span class="required">*</span></label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="form-group text-center">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle"></i> Create
                            </button>
                            <a asp-action="RemittanceTypes" class="btn btn-secondary ms-2">
                                <i class="bi bi-arrow-left-circle"></i> Back
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
